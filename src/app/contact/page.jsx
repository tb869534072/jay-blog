import React from "react";
import ContactClient from "./contactClient";

export const metadata = {
  title: "GamerBlog | Contact",
  description: "Contact with author.",
}

export default function ContactPage() {
  return <ContactClient />;
}