import React from "react";
import LoginClient from "./loginClient";

export const metadata = {
  title: "GamerBlog | Login",
  description: "Login to write a new post or comment.",
}

export default function LoginPage() {
  return <LoginClient />;
}